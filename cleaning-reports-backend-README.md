Cleaning Reports — Backend scaffold

Quick start:

1. Set environment variables in `.env` (see `.env.example`).
2. Install dependencies:

```bash
cd cleaning-reports/backend
npm install
```

3. Run in dev mode:

```bash
npm run dev
```
